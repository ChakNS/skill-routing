# Routing Fixtures

Use these as manual regression prompts.

## Feature

Prompt: "Add CSV export to this table and include a regression test."

Expected route: `feature_from_idea`.

## Bug

Prompt: "The login test is failing after the auth refactor. Find the root cause and fix it."

Expected route: `bug_fix`.

## Frontend

Prompt: "Redesign this React settings page and verify the mobile layout in browser."

Expected route: `frontend_ui_change`.

## Backend

Prompt: "Add a Supabase RLS policy and update the API tests."

Expected route: `backend_data_change`.

## Skill Development

Prompt: "Create an open-source routing skill with a content router and coding router."

Expected route: `skill_development`.
